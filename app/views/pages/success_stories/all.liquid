---
title: Return all the success stories
published: true
response_type: application/json
handle: api_success_stories
---
{
  "success_stories": [
    {% for success_story in contents.success_stories join: "," %}
    {
      "title": {{ success_story.title | json }},,
      "before_image": {
        "url": {{ success_story.before_image.url | prepend: host | prepend: "http://" | json }}
      },,
      "after_image": {
        "url": {{ success_story.after_image.url | prepend: host | prepend: "http://" | json }}
      }
    }
    {% endfor %}
  ]
}