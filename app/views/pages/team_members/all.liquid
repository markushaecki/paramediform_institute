---
title: Return all the team members
published: true
response_type: application/json
handle: api_team_members
---
{
  "team_members": [
    {% for member in contents.team_members join: "," %}
    {
      {{ member | json: "name,title" }},,
      "picture": {
        "url": {{ member.picture.url | prepend: host | prepend: "http://" | json }}
      }
    }
    {% endfor %}
  ]
}